//: [Previous](@previous)

import Foundation

/* Design an algorithm capable of determining whether a number is in the range between A and B (inclusive). Note: Both the number N and A and B are specified by the user. */

let a: Int = 3
let b: Int = 10
let num: Int = 7

if num >= a && num <= b {
    print("The number is in the range between \(a) and \(b).")
} else {
    print("The number is not in the range between \(a) and \(b).")
}

//: [Next](@next)
